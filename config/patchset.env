# 本项目 patch-set 版本。
# 默认留空 —— our_tag 直接对应上游 NDK 版本（如 r30-30.0.15729638-beta2），不带后缀。
# 仅当 obfuscation/ overlay 或构建参数变化、但上游 NDK 未变、需要就同一 NDK 版本
# 重新发布时，才设值（p1 -> p2 ...）：discover 会据此生成一个新的、不可变的 Release
# tag 重新发布（Release 不覆盖历史）。留空时不追加 patchset 段。
PATCHSET_VERSION=
