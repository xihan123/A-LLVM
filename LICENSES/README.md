# 第三方许可

仓库根目录的 `LICENSE` 适用于本项目新增代码。overlay 中保留的上游代码按其原始许可分发：

- `LLVM-UIUC.txt`：LLVM 旧版文件头所引用的 University of Illinois/NCSA 许可证；
- `LIBTOMCRYPT-UNLICENSE.txt`：`CryptoUtils` 中源自 LibTomCrypt 的 AES 实现所对应的公有领域声明。

文件头中的 `LICENSE.TXT` 引用对应 `LLVM-UIUC.txt`。分发二进制时，相关版权和免责声明随 `LICENSES/` 一起提供。
